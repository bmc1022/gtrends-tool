en:
  activerecord:
    attributes:
      admin:
        login: "username"
    errors:
      models:
        user:
          attributes:
            current_password:
              invalid: "Current password is invalid"
            reset_password_token:
              invalid: "Reset password token is invalid"
